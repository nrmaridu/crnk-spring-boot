openapi: 3.0.1
info:
  title: Title From Template File
  description: OpenAPI document with autogenerated paths, components, responses, etc.
  version: 0.1.0
paths:
  /projects/{id}:
    get:
      description: This is the description
      summary: This summary of projects
      responses:
        "200":
          $ref: '#/components/responses/ProjectsResourceReferenceResponse'
          description: OK
        "202":
          $ref: '#/components/responses/ProjectsResourceReferencesResponse'
        "204":
          $ref: '#/components/responses/NoContent'
servers:
  - url: 'http://127.0.0.1:8082/api'
    description: Production server (uses live data)
  - url: 'http://devserver/event/'
    description: Sandbox server (uses develop data)